services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - /srv/data/cooking:/app/data
    env_file:
      - ./backend/.env
  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: on-failure
