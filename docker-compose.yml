services:
  backend:
    build: ./backend
    volumes:
      - /srv/data/cooking:/app/data
    env_file:
      - ./backend/.env
    restart: unless-stopped
  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
